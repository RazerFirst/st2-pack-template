---
name: service_restart
description: Restart service 
runner_type: local-shell-cmd
entry_point: ""
enabled: true
parameters:
  sudo:
    description: "Run command with sudo"
    type: boolean
    immutable: true
    default: true
  kwarg_op:
    immutable: true
  cmd:
    description: "Command to run"
    type: string
    immutable: true
    default: "servoce {{service_name}} restart'"
  hosts:
  service_name:
    description: "Service to restart"
    type: string
    required: true
