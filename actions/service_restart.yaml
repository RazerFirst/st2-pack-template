name: notify
runner_type: "local-shell-cmd"
description: Restart service.
enabled: true
entry_point: bash_scripts/restart_service.sh
parameters:
  message:
    type: string
    description: "Message to send"
required: true
