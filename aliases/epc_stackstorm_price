---
name: epc_stackstorm_price
description: "Get Stackstorm price in EPC"
runner_type: local-shell-cmd
entry_point: ""
enabled: true
parameters:
  sudo:
    description: "Run command with sudo"
    immutable: true
    default: true
  kwarg_op:
    immutable: true
  cmd:
    description: "Command to run"
    type: string
    immutable: true
    default: "/home/kostiantyn_pokotilov@epam.com/epc/maestro-cli/bin/or2report -p Personal -r EPAM-MSQ3 -m `date -d "$D" '+%m'` -y `date -d "$D" '+%Y'` -t quota"
  hosts:
    description: "Ansible hosts to ping"
    type: string
    required: true
